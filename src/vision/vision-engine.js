export const VISION_SCHEMA='randstudio.vision/v1';
export const VISION_TOOLS=Object.freeze(['track','mask','background-remove','object-remove','upscale']);
export function createVisionJob(tool,{clipId=null,timeRange=null,prompt='',options={}}={}){if(!VISION_TOOLS.includes(tool))throw new Error(`Vision tool non supportato: ${tool}`);return{schema:VISION_SCHEMA,id:id(),tool,clipId,timeRange,prompt:String(prompt||''),options:structuredClone(options),status:'queued',createdAt:new Date().toISOString()}}
export class VisionProviderRegistry{constructor(){this.providers=new Map()}register(provider){if(!provider?.id||typeof provider.run!=='function')throw new Error('Vision provider non valido');this.providers.set(provider.id,provider);return this}list(){return[...this.providers.values()].map(p=>({id:p.id,name:p.name||p.id,local:!!p.local,tools:p.tools||[]}))}async run(id,job){const p=this.providers.get(id);if(!p)throw new Error(`Vision provider non trovato: ${id}`);if(Array.isArray(p.tools)&&!p.tools.includes(job.tool))throw new Error(`${p.id} non supporta ${job.tool}`);return p.run(job)}}
export class GatewayVisionProvider{constructor(endpoint){this.id='gateway-vision';this.name='AI Gateway Vision';this.endpoint=String(endpoint||'').replace(/\/$/,'');this.tools=[...VISION_TOOLS]}async run(job){if(!this.endpoint)throw new Error('Endpoint Vision non configurato');const r=await fetch(`${this.endpoint}/vision`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(job)});if(!r.ok)throw new Error(`Vision gateway HTTP ${r.status}`);return r.json()}}
export function createTrack(points=[],options={}){return{schema:'randstudio.track/v1',mode:options.mode||'point',points:(points||[]).map(p=>({t:Number(p.t)||0,x:Number(p.x)||0,y:Number(p.y)||0,confidence:Number(p.confidence??1)})).sort((a,b)=>a.t-b.t)}}
export function createMask(points=[],options={}){return{schema:'randstudio.mask/v1',type:options.type||'polygon',feather:Math.max(0,Number(options.feather)||0),invert:!!options.invert,points:(points||[]).map(p=>({x:Number(p.x)||0,y:Number(p.y)||0}))}}
function id(){return globalThis.crypto?.randomUUID?.()||`vision-${Date.now()}-${Math.random().toString(16).slice(2)}`}
